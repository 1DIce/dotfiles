- create:
    - ~/.config/fish

- link:
    - ~/.config/fish/config.fish:
      path: fish/config.fish

- crossplatform-shell:
    - command: sudo add-apt-repository ppa:fish-shell/release-4 -y && sudo apt update && sudo apt install fish -y
      platform: linux
    - command: brew install fish
      platform: darwin
